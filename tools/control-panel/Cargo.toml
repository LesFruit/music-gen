[package]
name = "control-panel"
version = "0.1.0"
edition = "2024"
description = "Rust CLI control panel for remote GPU/podman nodes over Tailscale"

[dependencies]
anyhow = "1.0"
clap = { version = "4.5", features = ["derive"] }
serde = { version = "1.0", features = ["derive"] }
toml = "0.9"
